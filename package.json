{
  "name": "artisan-auction-platform",
  "version": "1.0.0",
  "description": "A minimalist, elegant auction platform for handcrafted products",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["auction", "handcrafted", "artisan", "mern", "react", "node"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}